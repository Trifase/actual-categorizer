[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "actual-categorizer"
version = "0.1.5"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "actualpy>=0.22.3",
    "python-dotenv>=1.2.2",
    "rich>=15.0.0",
]

[project.scripts]
categorize = "categorize:run_categorization"
cleanup = "cleanup:run_cleanup"
remove-empty-payees = "remove_empty_payees:run_remove_empty"

[tool.uv]
package = true

[tool.hatch.build.targets.wheel.force-include]
"categorize.py" = "categorize.py"
"cleanup.py" = "cleanup.py"
"remove_empty_payees.py" = "remove_empty_payees.py"
"locales" = "locales"
